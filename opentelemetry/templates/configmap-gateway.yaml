{{- if .Values.gateway.enabled }}
{{- /*
  Génère le ConfigMap GATEWAY avec filtrage des receivers/processors/exporters/connectors
  selon les pipelines déclarés dans `gateway.pipelinesFile`.

  Les `extensions` sont rendues sans filtrage (elles ne sont pas dans les
  pipelines). Toutes celles trouvées sont chargées et listées dans
  `service.extensions`.
*/ -}}

{{- $conf := dict
  "receivers"  dict
  "processors" dict
  "exporters"  dict
  "connectors" dict
  "extensions" dict
-}}

{{- $kinds := list "receivers" "processors" "exporters" "connectors" "extensions" }}

  # Fusion ordonnée des fragments : pour chaque kind, parcours la liste des
  # dossiers et mergeOverwrite chacun par-dessus le résultat courant. Le
  # dernier dossier de la liste écrase les précédents en cas de conflit.
{{- range $kind := $kinds }}
  {{- $dirs := default (list) (index $.Values.fragmentDirs $kind) }}
  {{- range $rawDir := $dirs }}
    {{- $path := trimPrefix "./" $rawDir }}
    {{- if $path }}
      {{- $files := $.Files.Glob (printf "%s/*.y*ml" $path) }}
      {{- range $filePath, $fileContent := $files }}
        {{- $yaml := fromYaml (toString $fileContent) }}
        {{- if $yaml }}
          {{- $_ := set $conf $kind (mergeOverwrite (get $conf $kind) $yaml) }}
        {{- end }}
      {{- end }}
    {{- end }}
  {{- end }}
{{- end }}

  # Chargement du fichier pipelines
{{- $pipelines := fromYaml (.Files.Get (trimPrefix "./" .Values.gateway.pipelinesFile)) }}

  # Extraire les receivers/processors/exporters/connectors réellement utilisés
{{- $usedReceivers := list }}
{{- $usedProcessors := list }}
{{- $usedExporters := list }}

{{- range $pName, $pConf := $pipelines.pipelines }}
  {{- range $r := $pConf.receivers }}
    {{- $usedReceivers = append $usedReceivers $r }}
  {{- end }}
  {{- range $pr := $pConf.processors }}
    {{- $usedProcessors = append $usedProcessors $pr }}
  {{- end }}
  {{- range $e := $pConf.exporters }}
    {{- $usedExporters = append $usedExporters $e }}
  {{- end }}
{{- end }}

  # Filtrer les blocs réellement nécessaires
{{- $filteredReceivers := dict }}
{{- range $name, $confItem := (get $conf "receivers") }}
  {{- if mustHas $name $usedReceivers }}
    {{- $_ := set $filteredReceivers $name $confItem }}
  {{- end }}
{{- end }}

{{- $filteredProcessors := dict }}
{{- range $name, $confItem := (get $conf "processors") }}
  {{- if mustHas $name $usedProcessors }}
    {{- $_ := set $filteredProcessors $name $confItem }}
  {{- end }}
{{- end }}

{{- $filteredExporters := dict }}
{{- range $name, $confItem := (get $conf "exporters") }}
  {{- if mustHas $name $usedExporters }}
    {{- $_ := set $filteredExporters $name $confItem }}
  {{- end }}
{{- end }}

  # connectors sont utilisés comme receivers/exporters
{{- $filteredConnectors := dict }}
{{- range $name, $confItem := (get $conf "connectors") }}
  {{- if or (mustHas $name $usedReceivers) (mustHas $name $usedExporters) }}
    {{- $_ := set $filteredConnectors $name $confItem }}
  {{- end }}
{{- end }}

  # Extensions : pas de filtrage par pipeline. `service.extensions` reçoit la
  # liste plate des keys, ordre stable.
{{- $extensions := get $conf "extensions" }}
{{- $extensionKeys := list }}
{{- range $name, $_ := $extensions }}
  {{- $extensionKeys = append $extensionKeys $name }}
{{- end }}
{{- $extensionKeys = sortAlpha $extensionKeys }}

  # Construction finale
{{- $serviceBlock := dict "pipelines" $pipelines.pipelines }}
{{- if $extensionKeys }}
  {{- $_ := set $serviceBlock "extensions" $extensionKeys }}
{{- end }}

{{- $gatewayConfig := dict
  "receivers"  $filteredReceivers
  "processors" $filteredProcessors
  "exporters"  $filteredExporters
  "connectors" $filteredConnectors
  "extensions" $extensions
  "service"    $serviceBlock
-}}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-gateway-configmap
  namespace: {{ .Values.namespace }}
data:
  config.yaml: |
{{ toYaml $gatewayConfig | indent 4 }}
{{- end }}
